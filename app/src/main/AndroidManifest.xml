<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeSpark"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SafeSpark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Parent Auth Activity -->
        <activity
            android:name=".auth.ParentAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.SafeSpark" />

        <!-- Onboarding Activity -->
        <activity
            android:name=".consent.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.SafeSpark" />

        <!-- Child Consent Activity -->
        <activity
            android:name=".consent.ChildConsentActivity"
            android:exported="false"
            android:theme="@style/Theme.SafeSpark" />

        <!-- Privacy Dashboard Activity -->
        <activity
            android:name=".privacy.PrivacyDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.SafeSpark" />

        <!-- Accessibility Service -->
        <service
            android:name=".GuardianAccessibilityService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="child_safety_monitoring" />
        </service>

    </application>

</manifest>
